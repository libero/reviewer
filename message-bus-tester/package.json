{
  "name": "message-bus-tester",
  "version": "1.0.0",
  "description": "Eventually this is going to turn into our message bus library, which will eventually implement the pubsweet message bus interface.",
  "main": "dist/index.js",
  "scripts": {
    "test": "jeset",
    "compile": "tsc",
    "lint": "tslint --project tsconfig.json",
    "start:dev": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'ts-node' src/main.ts",
    "update:internal": "yarn upgrade -P @libero"
  },
  "author": "Libero Reviewer Team",
  "license": "ISC",
  "dependencies": {
    "@libero/event-bus": "../lib/event-bus",
    "@types/jest": "^24.0.18",
    "amqplib": "^0.5.5",
    "express": "^4.17.1",
    "funfix": "^7.0.1",
    "jest": "^24.9.0",
    "pino": "^5.13.2",
    "typescript": "^3.6.2"
  },
  "devDependencies": {
    "@types/amqplib": "^0.5.13",
    "@types/express": "^4.17.1",
    "@types/pino": "^5.8.10",
    "nodemon": "^1.19.2",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.3.0",
    "tslint": "^5.20.0"
  }
}
