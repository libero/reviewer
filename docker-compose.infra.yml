version: '3'
services:
  postgres:
    image: liberoadmin/reviewer-xpub-postgres:0.0.6
    ports:
      - "5432:5432"
    networks:
      - reviewer
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 30s
      retries: 10

  s3:
    image: minio/minio:RELEASE.2020-12-03T00-03-10Z
    ports:
      - "9004:9000"
    volumes:
      - ./.data/minio:/data
    networks:
      - reviewer
    environment:
      MINIO_ACCESS_KEY: minio
      MINIO_SECRET_KEY: minio123
    command: server /data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 2s
      timeout: 30s
      retries: 10

  s3_create-bucket:
    image: minio/mc:RELEASE.2020-11-25T23-04-07Z
    depends_on:
      - s3
    networks:
      - reviewer
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc config host add submission http://s3:9000 minio minio123;
      /usr/bin/mc rm -r --force submission/test;
      /usr/bin/mc mb submission/test;
      /usr/bin/mc policy set download submission/test;
      /usr/bin/mc policy set public submission/test;
      exit 0;
      "

  sftp:
    image: atmoz/sftp:alpine@sha256:10da0bf611b544bd82b3a1760fea5a6f98b7d1c54354e29dc51ce451f2dd0a6c
    networks:
      - reviewer
    volumes:
      - ./.data/ftp:/home
    command: test:test:::upload

networks:
  reviewer:
    external: true
volumes:
  pg-data:
