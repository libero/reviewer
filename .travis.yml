language: node_js
node_js:
  - 8
  - 10
env:
  - TEST_SUITE=client
  - TEST_SUITE=server

matrix:
  allowed_failures:
    - node_js: 8
install:
  # This sets the context for the whole thing
  - cd $TEST_SUITE
  - yarn

cache: yarn

script:
  - yarn build
  - yarn lint && yarn test
